import styled from "styled-components";

const Title = styled.h2`
  color: #bcd9ff;
  font-family: "Lexend";
  font-size: 6rem;
  font-weight: 600;
  font-style: normal;
  margin: 0;
`;

export default Title;
