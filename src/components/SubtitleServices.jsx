import styled from "styled-components";

const SubtitleService = styled.h2`
  color: #bcd9ff;

  font-family: Lato;
  font-size: 3rem;
  font-style: normal;
  font-weight: 600;
`;

export default SubtitleService;
